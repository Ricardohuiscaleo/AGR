---
// =====================================================================
// COMPONENTE ESPECIALISTA PARA 'contacto-part3'
// =====================================================================

import BookingCalendar from '../calendar/BookingCalendar.jsx';

interface Props {
  section: any;
}
const { section } = Astro.props;
---

<!-- Contenedor sin márgenes para el calendario de reservas -->
<div class="booking-container">
  <BookingCalendar
    client:only="react"
    calendarId="ricardo.huiscaleo@gmail.com"
    title=""
    description=""
    accentColor="#7c3aed"
    workingHoursStart={9}
    workingHoursEnd={22}
    slotDuration={60}
    timeZone="America/Santiago"
    sessionDuration={30}
    sessionPrice="FREE"
    className="booking-calendar-full-width"
  />
</div>

<style>
  /* Contenedor sin márgenes laterales y con altura completa */
  .booking-container {
    display: block;
    padding: 0;
    margin: 0;
    width: 100%;
    height: 100%;
    max-width: 100%;
    overflow: hidden;
    /* Borde lila */
    border: 2px solid #8a5cf68d;
    border-radius: 12px;
  }

  /* ...existing code... */
</style>
