---
// =====================================================================
// COMPONENTE PARA 'soluciones-part2' - Casos de Uso por Segmentos
// =====================================================================
interface Props {
  section: any;
}
const { section } = Astro.props;

const casosDeUso = [
  {
    icono: '⚖️',
    segmento: 'Abogados',
    titulo: 'Gestión Legal Inteligente',
    descripcion: 'Automatiza búsquedas jurisprudenciales, redacción de contratos y consultas de normativas. Tu asistente legal que conoce toda tu documentación.',
    beneficios: ['Búsqueda instantánea en expedientes', 'Redacción automática de documentos', 'Consultas normativas 24/7'],
    colorAccent: '#00ff41'
  },
  {
    icono: '🏥',
    segmento: 'Clínicas',
    titulo: 'Asistente Médico RAG',
    descripcion: 'Optimiza gestión de historiales, citas y consultas médicas. Acceso instantáneo a protocolos y información de pacientes.',
    beneficios: ['Gestión de historiales médicos', 'Programación inteligente de citas', 'Consultas de protocolos médicos'],
    colorAccent: '#00ff41'
  },
  {
    icono: '🎓',
    segmento: 'Educación',
    titulo: 'Campus Inteligente',
    descripcion: 'Centraliza información académica, procedimientos y consultas estudiantiles. Tu asistente educativo que conoce toda la institución.',
    beneficios: ['Consultas académicas automatizadas', 'Gestión de procedimientos', 'Soporte estudiantil 24/7'],
    colorAccent: '#00ff41'
  },
  {
    icono: '📦',
    segmento: 'Logística',
    titulo: 'Operaciones Optimizadas',
    descripcion: 'Automatiza seguimiento de envíos, inventarios y procedimientos operativos. Control total de tu cadena logística.',
    beneficios: ['Tracking automático de envíos', 'Gestión inteligente de inventarios', 'Optimización de rutas'],
    colorAccent: '#00ff41'
  },
  {
    icono: '🔧',
    segmento: 'Servicios',
    titulo: 'Gestión de Servicios Pro',
    descripcion: 'Centraliza manuales técnicos, procedimientos y atención al cliente. Tu base de conocimiento técnico siempre disponible.',
    beneficios: ['Manuales técnicos accesibles', 'Procedimientos estandarizados', 'Soporte técnico inteligente'],
    colorAccent: '#00ff41'
  },
  {
    icono: '🏭',
    segmento: 'Manufactura',
    titulo: 'Producción Inteligente',
    descripcion: 'Optimiza procesos productivos, control de calidad y mantenimiento. Tu asistente de producción experto.',
    beneficios: ['Control de calidad automatizado', 'Gestión de mantenimiento', 'Optimización de procesos'],
    colorAccent: '#00ff41'
  }
];
---

<!-- Contenedor principal con estructura mínima como el ejemplo -->
<div
  id="soluciones-part2-timeline"
  class="w-full relative z-10"
  style="height: 100%; position: relative; margin: 2rem; margin-left: 1rem; margin-right: 1rem;"
>
  <div class="timeline-wrapper">
    <h2
      class="text-7xl sm:text-5xl md:text-6xl lg:text-8xl xl:text-9xl font-bold text-white mb-4 md:mb-6 leading-tight"
    >
      Casos de Uso Inteligentes por Segmento
    </h2>
    <div class="max-w-7xl">
      <p
        class="text-base sm:text-lg md:text-2xl lg:text-3xl xl:text-4xl text-gray-300 leading-relaxed"
      >
        Hoy{' '}
        {
          new Date().toLocaleDateString('es-ES', {
            weekday: 'long',
            day: 'numeric',
            month: 'long',
          })
        }{' '}
        implementar{' '}
        <span
          class="animated-gradient-text"
          style="background-image: linear-gradient(45deg, #00ff41 0%, #5fff78 25%, #85ff99 50%, #5fff78 75%, #00ff41 100%); -webkit-background-clip: text; background-clip: text; color: transparent; display: inline;"
        >
          soluciones RAG especializadas
        </span>{' '}
        es{' '}
        <span
          class="inline-block bg-gradient-to-r from-green-400 via-emerald-400 to-green-400 bg-[length:200%_100%] text-transparent bg-clip-text font-semibold italic transform -rotate-1 animate-shimmer"
        >
          rentable y alcanzable
        </span>{' '}
        🚀.
      </p>
    </div>
  </div>

  <!-- Carrusel de Casos de Uso -->
  <div class="carrusel-container mt-16">
    <!-- Contenedor scrolleable con mask para desvanecimiento -->
    <div class="carrusel-scroll" id="carrusel-casos">
      {casosDeUso.map((caso, index) => (
        <div class="caso-card">
          <!-- Header de la tarjeta -->
          <div class="caso-header">
            <div class="caso-icono">
              {caso.icono}
            </div>
            <div class="caso-segmento">{caso.segmento}</div>
          </div>

          <!-- Contenido -->
          <div class="caso-content">
            <h3 class="caso-titulo">{caso.titulo}</h3>
            <p class="caso-descripcion">{caso.descripcion}</p>

            <!-- Lista de beneficios -->
            <ul class="caso-beneficios">
              {caso.beneficios.map((beneficio) => (
                <li class="caso-beneficio">
                  <span class="beneficio-bullet">▸</span>
                  {beneficio}
                </li>
              ))}
            </ul>
          </div>

          <!-- CTA -->
          <div class="caso-cta">
            <a 
              href="#contacto-part2"
              class="cta-button"
            >
              <span class="cta-text">Implementar Solución</span>
              <span class="cta-arrow">→</span>
            </a>
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- Indicador de scroll (opcional) -->
  <div class="scroll-indicator">
    <p class="text-sm text-gray-400 text-center mt-8">
      ← Desliza horizontalmente para ver más soluciones →
    </p>
  </div>
</div>

<style>
  /* Contenedor principal del carrusel */
  .carrusel-container {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  /* Contenedor scrolleable horizontal */
  .carrusel-scroll {
    display: flex;
    gap: 24px;
    padding: 40px 80px; /* Aumentar padding vertical y horizontal para el zoom */
    overflow-x: auto;
    scroll-behavior: smooth;
    scrollbar-width: none; /* Firefox */
    -ms-overflow-style: none; /* IE/Edge */
    /* Aplicar mask para desvanecimiento lateral - COMO EN EMPRESA PARTE 4 */
    -webkit-mask: linear-gradient(90deg, transparent, white 20%, white 80%, transparent);
    mask: linear-gradient(90deg, transparent, white 20%, white 80%, transparent);
  }

  /* Ocultar scrollbar en webkit browsers */
  .carrusel-scroll::-webkit-scrollbar {
    display: none;
  }

  /* Cards del carrusel */
  .caso-card {
    background: rgba(0, 0, 0, 0.7);
    border: 2px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 24px;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
    min-width: 320px;
    max-width: 320px;
    flex-shrink: 0;
    height: auto;
  }

  .caso-card:hover {
    border-color: #00ff41;
    box-shadow: 0 8px 32px rgba(0, 255, 65, 0.2);
    transform: translateY(-8px) scale(1.05); /* Aumentar el efecto de zoom y elevación */
  }

  .caso-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #00ff41, #5fff78);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .caso-card:hover::before {
    transform: scaleX(1);
  }

  .caso-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
  }

  .caso-icono {
    font-size: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;
    height: 56px;
    background: rgba(0, 255, 65, 0.1);
    border-radius: 12px;
    border: 1px solid rgba(0, 255, 65, 0.3);
  }

  .caso-segmento {
    font-family: 'Courier New', monospace;
    font-size: 14px;
    font-weight: 700;
    color: #00ff41;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .caso-content {
    margin-bottom: 24px;
  }

  .caso-titulo {
    font-size: 20px;
    font-weight: 700;
    color: white;
    margin-bottom: 12px;
    line-height: 1.3;
  }

  .caso-descripcion {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.5;
    margin-bottom: 16px;
  }

  .caso-beneficios {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .caso-beneficio {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 6px;
    line-height: 1.4;
  }

  .beneficio-bullet {
    color: #00ff41;
    font-weight: 700;
    margin-top: 2px;
    flex-shrink: 0;
  }

  .caso-cta {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 20px;
  }

  .cta-button {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    background: rgba(0, 255, 65, 0.1);
    border: 1px solid rgba(0, 255, 65, 0.3);
    border-radius: 8px;
    padding: 12px 16px;
    color: #00ff41;
    font-family: 'Courier New', monospace;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: rgba(0, 255, 65, 0.2);
    border-color: #00ff41;
    color: white;
  }

  .cta-arrow {
    transition: transform 0.3s ease;
  }

  .cta-button:hover .cta-arrow {
    transform: translateX(4px);
  }

  /* Indicador de scroll */
  .scroll-indicator {
    margin-top: 20px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .carrusel-scroll {
      padding: 30px 40px; /* Mantener padding suficiente en móvil */
      gap: 16px;
    }

    .caso-card {
      min-width: 280px;
      max-width: 280px;
      padding: 20px;
    }

    .caso-icono {
      width: 48px;
      height: 48px;
      font-size: 28px;
    }

    .caso-titulo {
      font-size: 18px;
    }

    .fade-left, .fade-right {
      width: 80px;
    }
  }

  @media (max-width: 480px) {
    .carrusel-scroll {
      padding: 20px 30px; /* Padding mínimo pero suficiente */
    }

    .caso-card {
      min-width: 260px;
      max-width: 260px;
    }

    .fade-left, .fade-right {
      width: 60px;
    }
  }
</style>

<script>
  // Funcionalidad mejorada para scroll híbrido (horizontal + vertical)
  document.addEventListener('DOMContentLoaded', () => {
    const carrusel = document.getElementById('carrusel-casos') as HTMLElement;
    
    if (carrusel) {
      let isScrollingHorizontally = false;
      
      // Detectar cuando el scroll horizontal ha terminado
      function isAtHorizontalEnd(): boolean {
        const { scrollLeft, scrollWidth, clientWidth } = carrusel as any;
        return scrollLeft >= scrollWidth - clientWidth - 10;
      }
      
      function isAtHorizontalStart(): boolean {
        return (carrusel as any).scrollLeft <= 10;
      }
      
      // Manejar el scroll híbrido
      carrusel.addEventListener('wheel', (e: WheelEvent) => {
        const scrollingDown = e.deltaY > 0;
        const scrollingUp = e.deltaY < 0;
        
        // Si es scroll horizontal (deltaX), permitir scroll horizontal normal
        if (Math.abs(e.deltaX) > Math.abs(e.deltaY)) {
          return; // Permitir scroll horizontal normal
        }
        
        // Si es scroll vertical
        if (Math.abs(e.deltaY) > Math.abs(e.deltaX)) {
          // Si scrolleamos hacia abajo y estamos al final del carrusel horizontal
          if (scrollingDown && isAtHorizontalEnd()) {
            // NO prevenir el evento - permitir scroll vertical hacia abajo
            return;
          }
          
          // Si scrolleamos hacia arriba y estamos al inicio del carrusel horizontal
          if (scrollingUp && isAtHorizontalStart()) {
            // NO prevenir el evento - permitir scroll vertical hacia arriba
            return;
          }
          
          // Si estamos en el medio del carrusel, convertir scroll vertical en horizontal
          e.preventDefault();
          (carrusel as any).scrollLeft += e.deltaY;
        }
      }, { passive: false });

      // Indicar cuando hay más contenido por ver
      function updateScrollIndicator(): void {
        const scrollIndicator = document.querySelector('.scroll-indicator');
        if (scrollIndicator && carrusel) {
          const { scrollLeft, scrollWidth, clientWidth } = carrusel as any;
          const isAtStart = scrollLeft <= 10;
          const isAtEnd = scrollLeft >= scrollWidth - clientWidth - 10;
          
          let text = '';
          if (isAtStart && !isAtEnd) {
            text = 'Desliza hacia la derecha para ver más soluciones →';
          } else if (isAtEnd && !isAtStart) {
            text = 'Fin del carrusel - Puedes seguir bajando ↓';
          } else if (!isAtStart && !isAtEnd) {
            text = '← Desliza horizontalmente para ver más soluciones →';
          }
          
          const textElement = scrollIndicator.querySelector('p');
          if (textElement) {
            textElement.textContent = text;
          }
        }
      }

      carrusel.addEventListener('scroll', updateScrollIndicator);
      updateScrollIndicator(); // Llamada inicial
      
      // Agregar indicadores visuales para mejor UX
      function addScrollIndicators(): void {
        if (!carrusel) return;
        
        const { scrollLeft, scrollWidth, clientWidth } = carrusel as any;
        const isAtStart = scrollLeft <= 10;
        const isAtEnd = scrollLeft >= scrollWidth - clientWidth - 10;
        
        // Agregar clase para indicar estado del scroll
        carrusel.classList.toggle('at-start', isAtStart);
        carrusel.classList.toggle('at-end', isAtEnd);
        carrusel.classList.toggle('in-middle', !isAtStart && !isAtEnd);
      }
      
      carrusel.addEventListener('scroll', addScrollIndicators);
      addScrollIndicators(); // Llamada inicial
    }
  });
</script>