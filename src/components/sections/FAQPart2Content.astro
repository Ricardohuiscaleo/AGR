---
// =====================================================================
// SECCIÓN FAQ (PARTE 2) - LA EVOLUCIÓN DEL FAQ
// =====================================================================

// Importamos el wrapper del chat claro que combina con tonos rojos
import ChatInterfaceWrapper from '../ChatInterfaceWrapper.astro';

// Definimos las props que recibe
interface Props {
  section: any;
}
const { section } = Astro.props;
---

<div class="section-inner w-full p-0 m-0 relative overflow-hidden">
  <div class="w-full relative z-10">
    <!-- Container principal con padding responsive -->
    <div class="container mx-auto py-6 md:py-10 px-4 md:px-5">
      <!-- Grid principal: Texto a la izquierda, Chat a la derecha -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-16 items-center min-h-[600px]">
        <!-- LADO IZQUIERDO: Título "La evolución del FAQ" -->
        <div class="flex flex-col justify-center space-y-6 lg:space-y-8">
          <!-- Título principal en letras grandes -->
          <div class="space-y-4">
            <h2 class="text-7xl md:text-6xl lg:text-7xl xl:text-9xl font-bold leading-tight">
              <span class="block text-gray-900 mb-2">La evolución</span>
              <span
                class="block text-transparent bg-clip-text bg-gradient-to-r from-red-600 via-red-500 to-red-700"
              >
                del FAQ
              </span>
            </h2>

            <!-- Línea decorativa -->
            <div class="w-24 md:w-32 h-1 bg-gradient-to-r from-red-600 to-red-400 rounded-full">
            </div>
          </div>

          <!-- Subtítulo descriptivo -->
          <div class="space-y-4">
            <p class="text-lg md:text-xl lg:text-2xl text-gray-700 leading-relaxed">
              De preguntas estáticas a conversaciones inteligentes
            </p>
            <p class="text-base md:text-lg text-gray-600 leading-relaxed max-w-lg">
              Los FAQ tradicionales han evolucionado. Ahora puedes tener conversaciones naturales y
              obtener respuestas personalizadas en tiempo real gracias a la IA.
            </p>
          </div>

          <!-- Características destacadas -->
          <div class="space-y-3">
            <div class="flex items-center space-x-3">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <span class="text-gray-700 font-medium">Respuestas instantáneas 24/7</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <span class="text-gray-700 font-medium">Conversaciones naturales</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <span class="text-gray-700 font-medium">Aprendizaje continuo</span>
            </div>
          </div>

          <!-- Call to action -->
          <div class="pt-4"></div>
        </div>

        <!-- LADO DERECHO: Chat Interface Claro -->
        <div class="flex justify-center lg:justify-end">
          <div class="w-full max-w-lg lg:max-w-3xl xl:max-w-4xl">
            <!-- 🆕 Aumentado de xl:max-w-xl a xl:max-w-2xl -->
            <!-- Contenedor del chat con efectos visuales -->
            <div class="relative">
              <!-- Efecto de sombra/glow sutil -->
              <div class=""></div>

              <!-- Chat container -->
              <div class="">
                <ChatInterfaceWrapper />
              </div>

              <!-- Elementos decorativos -->
              <div
                class="absolute -top-4 -right-4 w-8 h-8 bg-red-500 rounded-full opacity-80 animate-pulse"
              >
              </div>
              <div
                class="absolute -bottom-4 -left-4 w-6 h-6 bg-red-300 rounded-full opacity-60 animate-pulse"
                style="animation-delay: 1s;"
              >
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Sección adicional: Comparativa FAQ tradicional vs IA -->
      <div class="mt-16 lg:mt-24">
        <div class="bg-gradient-to-r from-gray-200 to-gray-200 rounded-2xl p-8 md:p-12">
          <h3 class="text-2xl md:text-3xl font-bold text-center mb-8 text-gray-900">
            FAQ Tradicional vs FAQ con IA
          </h3>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <!-- FAQ Tradicional -->
            <div class="bg-white rounded-xl p-6 shadow-md">
              <div class="flex items-center mb-4">
                <div class="w-12 h-12 bg-gray-400 rounded-lg flex items-center justify-center mr-4">
                  <svg
                    class="w-6 h-6 text-white"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                    ></path>
                  </svg>
                </div>
                <h4 class="text-xl font-semibold text-gray-900">FAQ Tradicional</h4>
              </div>
              <ul class="space-y-3 text-gray-600">
                <li class="flex items-start space-x-2">
                  <span class="text-red-400 mt-1">×</span>
                  <span>Preguntas limitadas y predefinidas</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-400 mt-1">×</span>
                  <span>Respuestas estáticas</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-400 mt-1">×</span>
                  <span>Búsqueda manual</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-400 mt-1">×</span>
                  <span>No contextual</span>
                </li>
              </ul>
            </div>

            <!-- FAQ con IA -->
            <div class="bg-gradient-to-br from-white to-white rounded-xl p-6 shadow-md border-2">
              <div class="flex items-center mb-4">
                <div class="w-12 h-12 bg-red-500 rounded-lg flex items-center justify-center mr-4">
                  <svg
                    class="w-6 h-6 text-white"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"
                    ></path>
                  </svg>
                </div>
                <h4 class="text-xl font-semibold text-gray-900">FAQ con IA</h4>
              </div>
              <ul class="space-y-3 text-gray-700">
                <li class="flex items-start space-x-2">
                  <span class="text-red-500 mt-1">✓</span>
                  <span>Conversaciones naturales ilimitadas</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-500 mt-1">✓</span>
                  <span>Respuestas personalizadas</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-500 mt-1">✓</span>
                  <span>Comprensión inteligente</span>
                </li>
                <li class="flex items-start space-x-2">
                  <span class="text-red-500 mt-1">✓</span>
                  <span>Aprendizaje continuo</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  /* Animaciones personalizadas para esta sección */
  .section-inner {
    background: linear-gradient(135deg, #ffffff);
  }

  /* Efecto de hover MEJORADO para el título "del FAQ" */
  .section-inner h2 span.text-transparent {
    transition: all 0.3s ease;
    position: relative;
  }

  .section-inner h2:hover span.text-transparent {
    background: linear-gradient(45deg, #dc2626, #ef4444, #f87171, #dc2626);
    background-size: 300% 300%;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradient-shift 3s ease infinite;
    transform: scale(1.05);
  }

  @keyframes gradient-shift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  /* Animación de aparición para elementos */
  .section-inner > * {
    animation: fadeInUp 0.6s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
