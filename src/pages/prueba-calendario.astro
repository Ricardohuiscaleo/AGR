---
import Layout from '../layouts/Layout.astro';
import BookingCalendarWrapper from '../components/calendar/BookingCalendarWrapper.astro';

// N8n Webhook URLs
const webhookGetEvents =
  'https://primary-production-33e8.up.railway.app/webhook/156b71d9-9b89-4c39-9f45-3f098923054f';
const webhookCreateBooking =
  'https://primary-production-33e8.up.railway.app/webhook/20eb2a0b-10c6-44ca-9269-3e0597d40216';
const calendarId = 'ricardo.huiscaleo@gmail.com';
---

<Layout title="Sistema de Reservas Inteligente">
  <main class="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900">
    <!-- Hero Section del Sistema de Reservas -->
    <div class="hero-section">
      <div class="container mx-auto px-4 py-12">
        <div class="text-center max-w-4xl mx-auto">
          <h1 class="hero-title">🤖 Sistema de Reservas Inteligente</h1>
          <p class="hero-subtitle">
            Powered by IA y automatización n8n para una experiencia de reserva perfecta
          </p>
        </div>
      </div>
    </div>

    <!-- Contenedor principal del calendario con ancho completo -->
    <div class="calendar-section">
      <div class="container mx-auto px-4">
        <BookingCalendarWrapper
          calendarId={calendarId}
          title="Asesoría Personalizada en Soluciones IA"
          description="Agenda tu consultoría sobre implementación de sistemas RAG, automatización y soluciones de inteligencia artificial"
          accentColor="#3B82F6"
          workingHoursStart={9}
          workingHoursEnd={23}
          slotDuration={60}
          apiEndpoint="/api/calendar"
        />
      </div>
    </div>

    <!-- Sección de información técnica simplificada -->
    <div class="tech-info-section">
      <div class="container mx-auto px-4 py-12">
        <div class="max-w-4xl mx-auto">
          <div class="tech-card">
            <h2 class="tech-title">⚡ Integración Avanzada con n8n</h2>

            <div class="tech-grid">
              <div class="tech-item">
                <div class="tech-icon">🔗</div>
                <h3>Webhooks Inteligentes</h3>
                <p>Comunicación directa con Google Calendar sin exponer API keys</p>
              </div>

              <div class="tech-item">
                <div class="tech-icon">🛡️</div>
                <h3>Seguridad Avanzada</h3>
                <p>Autenticación centralizada y datos protegidos end-to-end</p>
              </div>

              <div class="tech-item">
                <div class="tech-icon">⚙️</div>
                <h3>Automatización Total</h3>
                <p>Flujos personalizables para notificaciones y validaciones</p>
              </div>
            </div>

            <!-- Información de endpoints colapsible -->
            <details class="endpoints-details">
              <summary class="endpoints-summary"> 📋 Configuración de Endpoints </summary>
              <div class="endpoints-content">
                <div class="endpoint-item">
                  <span class="endpoint-label">Obtener eventos:</span>
                  <code class="endpoint-code">{webhookGetEvents}</code>
                </div>
                <div class="endpoint-item">
                  <span class="endpoint-label">Crear reservas:</span>
                  <code class="endpoint-code">{webhookCreateBooking}</code>
                </div>
                <div class="endpoint-item">
                  <span class="endpoint-label">Calendar ID:</span>
                  <code class="endpoint-code">{calendarId}</code>
                </div>
              </div>
            </details>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>

<style>
  .hero-section {
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(147, 51, 234, 0.1) 100%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    color: white;
    margin-bottom: 1rem;
    text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    background: linear-gradient(135deg, #3b82f6, #8b5cf6, #06b6d4);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1.2;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 3vw, 1.25rem);
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.6;
    max-width: 600px;
    margin: 0 auto;
  }

  .calendar-section {
    padding: 2rem 0;
  }

  .tech-info-section {
    background: linear-gradient(135deg, rgba(15, 23, 42, 0.8) 0%, rgba(30, 41, 59, 0.8) 100%);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .tech-card {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 16px;
    padding: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }

  .tech-title {
    font-size: 2rem;
    font-weight: 700;
    color: white;
    text-align: center;
    margin-bottom: 2rem;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }

  .tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .tech-item {
    background: rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
  }

  .tech-item:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    background: rgba(255, 255, 255, 0.08);
  }

  .tech-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    display: block;
  }

  .tech-item h3 {
    color: white;
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .tech-item p {
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.5;
  }

  .endpoints-details {
    background: rgba(0, 0, 0, 0.2);
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .endpoints-summary {
    padding: 1rem 1.5rem;
    color: white;
    font-weight: 600;
    cursor: pointer;
    background: rgba(59, 130, 246, 0.1);
    transition: background-color 0.2s ease;
    user-select: none;
  }

  .endpoints-summary:hover {
    background: rgba(59, 130, 246, 0.2);
  }

  .endpoints-content {
    padding: 1.5rem;
    space-y: 1rem;
  }

  .endpoint-item {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .endpoint-label {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.875rem;
    font-weight: 500;
  }

  .endpoint-code {
    background: rgba(0, 0, 0, 0.3);
    padding: 0.5rem 0.75rem;
    border-radius: 6px;
    color: #10b981;
    font-family: 'Courier New', monospace;
    font-size: 0.75rem;
    word-break: break-all;
    border: 1px solid rgba(16, 185, 129, 0.2);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .calendar-section {
      padding: 1rem 0;
    }

    .tech-card {
      padding: 1.5rem;
      margin: 0 1rem;
    }

    .tech-title {
      font-size: 1.5rem;
    }

    .tech-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .tech-item {
      padding: 1rem;
    }

    .endpoint-item {
      flex-direction: column;
    }
  }

  /* Animaciones */
  .tech-card {
    animation: fadeInUp 0.8s ease-out;
  }

  .tech-item {
    animation: fadeInUp 0.6s ease-out;
    animation-fill-mode: both;
  }

  .tech-item:nth-child(1) {
    animation-delay: 0.1s;
  }
  .tech-item:nth-child(2) {
    animation-delay: 0.2s;
  }
  .tech-item:nth-child(3) {
    animation-delay: 0.3s;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
